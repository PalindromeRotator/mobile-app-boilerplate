import 'package:flutter/material.dart';

class FavoriteContactPage extends StatelessWidget {
  const FavoriteContactPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favorite contact Page'));
  }
}
